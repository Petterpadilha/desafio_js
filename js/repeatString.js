/*
Faça uma função chamada repeatString que recebe um parâmetro chamado, text do tipo string e uma 
chamada n do tipo number e retorna uma string, que é o parâmetro text repetido n vezes.
 */
function repeatString(text, n) {
    return text.repeat(n);
}

//Explicação

/*
Function recebe dois parâmetros, e precisa  retorna o texto e quantas vezes ele se repete,
para isso usamos repeat(numero).
*/

/*
console.log(repeatString("ok", 4));
console.log(repeatString("test", 2));
*/
// Concluida
