/*
Faça uma função chamada isNumber, que recebe um parâmetro chamado val, e 
retorna um boolean indicando, se o parâmetro é do tipo number.s
*/

function isNumber(val) {
    return typeof val === 'number'
}
/*
Função recebe um parâmetro, que precisa retornar um boolean indicando se o valor é verdadeiro ou falso,
para isso verificamos a igualdade restrita, valor recebido é do mesmo valor, e do mesmo tipo, se for
é verdadeiro, senão é falso.
*/
//console.log(isNumber(9.5));
//console.log(isNumber(1));
//console.log(isNumber(true));
//console.log(isNumber(null));
//console.log(isNumber("1"));

