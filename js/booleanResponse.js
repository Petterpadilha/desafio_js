/* 
Faça uma função chamada booleanResponse que recebe um parâmetro, chamado val do tipo boolean e retorna uma 
string, com valor sim para true e não para false.
*/

function booleanResponse(val) {
    return val ? "sim" : "não";
}
// Explicação
/*
Function recebe uma chamada, e precisa retorna um boolean sim ou nao,
utilizamos um operador ternario, quando receber true é sim, e quando receber false é não.
*/
//console.log(booleanResponse(true));
//console.log(booleanResponse(false));
// Concluida